'use strict';

angular.module('y')
  .config(function ($stateProvider) {
    $stateProvider
      .state('vehiculos-list', {
        url: 'n',
        template: '<vehiculos-list></vehiculos-list>'
      });
  });
